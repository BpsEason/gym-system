<?php

use Illuminate\Foundation\Testing\RefreshDatabase;

uses(RefreshDatabase::class)->in('Feature');

// Set up Pest environment (optional, but useful for quick test setup)
// You can add global setup/teardown here or in tests/Pest.php
// For example: uses(Tests\TestCase::class)->in('Feature', 'Unit');
